[build]
builder = "nixpacks"

[build.nixpacks]
pkgs = ["nodejs_20", "npm-9_x"]

[deploy]
startCommand = "node scripts/start.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

