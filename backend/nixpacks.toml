providers = ["node"]
[phases.setup]
nixPkgs = ["nodejs_18"]
[phases.install]
cmds = ["npm ci || npm install"]
[phases.build]
cmds = ["npx prisma generate"]
[start]
cmd = "npx prisma db push && node src/index.js"
